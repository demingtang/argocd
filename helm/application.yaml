apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: example
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/demingtang/argocd.git"
    targetRevision: HEAD
    path: helm
    helm:
      valueFiles:
        - "values.yaml"
        - "tests/server_test.yaml"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: "example"
  syncPolicy:
    automated: {}
    syncOptions:
      - CreateNamespace=true
