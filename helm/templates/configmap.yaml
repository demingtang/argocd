apiVersion: v1
kind: ConfigMap
metadata:
  name: vmagent-config
  namespace: {{ .Values.namespace }}
data:
  vmagent.yaml: |
    global:
      scrape_interval: {{ .Values.config.scrape_interval }}

    scrape_configs:
      - job_name: '{{ .Values.config.job_name }}'
        static_configs:
          - targets:
{{- range .Values.config.targets }}
              - '{{ . }}'
{{- end }}
            labels:
{{- range $key, $value := .Values.config.labels }}
              {{ $key }}: {{ $value }}
{{- end }}